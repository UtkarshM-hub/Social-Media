import React, { Fragment } from "react";
import LoginContainer from "../Components/LoginPage/LoginContainer/JS/LoginContainer";

const Login = () => {
  return (
    <Fragment>
      <LoginContainer />
    </Fragment>
  );
};

export default Login;
